{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend/dist",
        "buildCommand": "cd frontend && npm install && npm run build"
      }
    },
    {
      "src": "backend/vercel.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "cd backend && npm install && node server.cjs"
      }
    }
  ]
}
